
<div class="container">
    <div class="row">
        <h2>Welcome back Admin</h2>
        <div class="col-md">
            <h4>For recycle section</h4>
            <% easyrecicles.forEach(recycle => { %>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card'title"><%= recycle.name %> <span class="badge bg-secondary"><%= recycle.items %></span></h5>
                        <h6 class="card-subtitle mb-2 text-muted">Phone: <%= recycle.phone %>; Address: <%= recycle.address %></h6>
                        <p class="card-text"><%= recycle.description %></p>
                    </div>
                </div>
            <% }); %>
        </div>
        <div class="col-md">
            <h4>For recycle points</h4>
        </div>
    </div>
</div>